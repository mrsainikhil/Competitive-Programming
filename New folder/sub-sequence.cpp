#include<bits/stdc++.h>

using namespace std;

int main(){
	int s1, s2;
	cin>>s1>>s2;
	vector<int> vec1(s1);
	vector<int> vec2(s2);
	for(int i = 0; i < s1; i++)
		cin>>vec[i];
	for(int i = 0; i < s2; i++)
		cin>>vec[i];
	
	return 0;
}